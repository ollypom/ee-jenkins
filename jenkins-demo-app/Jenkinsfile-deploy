def demo-app-url = 'dtr.olly.dtcntr.net/fakeco-prod/demo-app' 

node("docker") {
    stage "deploy"
    sh "cd /home/jenkins && \
    source env.sh && \
    docker service update --image ${demo-app-url}:latest demo"
}
